@using CryptoInvestment.Helpers
<nav class="navbar-default navbar-static-side" role="navigation">
    <div class="sidebar-collapse">
        <ul class="nav metismenu" id="side-menu">
            <li class="nav-header">
                <div class="dropdown profile-element">
                    <a data-toggle="dropdown" class="dropdown-toggle" href="#">
                        <span class="block m-t-xs font-bold">
                            @if (User.Identity!.IsAuthenticated)
                            {
                                <text>@User.Identity.Name</text>
                            }
                            else
                            {
                                <text>Guest</text>
                            }
                        </span>
                        <span class="text-muted text-xs block">Opciones<b class="caret"></b></span>
                    </a>
                    <ul class="dropdown-menu animated fadeInRight m-t-xs" style="position: absolute; top: 91px; left: 0px; will-change: top, left;">
                        <li><a class="dropdown-item" href="@Url.Action("CustomerConfiguration", "Crypto")">Configuración</a></li>
                        <li><a class="dropdown-item" href="#">Logout</a></li>
                    </ul>
                </div>
                <div class="logo-element">
                    GHR
                </div>
            </li>
            <li class="@Html.IsSelected(controller: "Crypto", action: "Dashboard")">
                <a href="@Url.Action("Dashboard", "Crypto")"><i class="fa fa-briefcase"></i> <span class="nav-label">Activos</span></a>
            </li>
            <li class="@Html.IsSelected(controller: "Crypto", action: "Deposit")">
                <a href="@Url.Action("Deposit", "Crypto")"><i class="fa fa-bank"></i> <span class="nav-label">Depositos</span></a>
            </li>
            <li class="@Html.IsSelected(controller: "Crypto", action: "Withdraw")">
                <a href="@Url.Action("Withdraw", "Crypto")"><i class="fa fa-money"></i> <span class="nav-label">Retiros</span></a>
            </li>
            <li class="@Html.IsSelected(controller: "Crypto", action: "Referral")">
                <a href="@Url.Action("Referral", "Crypto")"><i class="fa fa-users"></i> <span class="nav-label">Referidos</span></a>
            </li>
            <li class="@Html.IsSelected(controller: "Crypto", action: "Actives")">
                <a href="@Url.Action("Movement", "Crypto")"><i class="fa fa-exchange"></i> <span class="nav-label">Movimientos</span></a>
            </li>
        </ul>
    </div>
</nav>